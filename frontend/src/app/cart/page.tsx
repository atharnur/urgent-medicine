"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {api} from "../../lib/api";
export default function Cart(){const[data,setData]=useState<any[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{api<any>("/cart").then(x=>setData(x.data||[])).catch(e=>setError(e.message||"Unable to load your cart.")).finally(()=>setLoading(false))},[]);return <main className="page"><div className="container"><div className="section-title" style={{marginTop:0}}><div><div className="eyebrow">Shopping cart</div><h2>Review your medicines</h2><p>Confirm quantities before moving to secure checkout.</p></div><span className="pill">{data.length} line items</span></div>{loading?<div className="card empty">Loading cart…</div>:error?<div className="card"><p className="error">{error}</p></div>:!data.length?<div className="card empty"><h3 style={{color:"var(--ink)"}}>Your cart is empty</h3><p>Start by finding a medicine in the catalogue.</p><Link className="btn primary" href="/medicines">Browse medicines</Link></div>:<div className="summary"><div className="card">{data.map(i=><div className="summary-row" key={i.id}><div><b>{i.tradeName}</b><div className="muted">Quantity: {i.quantity}</div></div><span>{i.totalBdt?`BDT ${i.totalBdt}`:"—"}</span></div>)}</div><aside className="card"><h3 style={{marginTop:0}}>Next step</h3><p className="muted">At confirmation, the backend recalculates order totals and applies the fixed BDT 220 delivery charge.</p><Link className="btn primary" style={{width:"100%"}} href="/checkout">Proceed to checkout</Link></aside></div>}</div></main>}
