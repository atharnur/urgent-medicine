"use client";
import { FormEvent, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { api } from "../../lib/api";
export default function ResetPassword(){const router=useRouter(),p=useSearchParams();const[token,setToken]=useState(p.get("token")??""),[pw,setPw]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");try{await api("/auth/reset-password",{method:"POST",body:JSON.stringify({token,password:pw,confirmPassword:confirm})});setDone(true);setTimeout(()=>router.push("/login"),900)}catch(e:any){setError(e.message)}}return <main className="container"><div className="card" style={{maxWidth:480,margin:"40px auto"}}><h1>Reset password</h1><form onSubmit={submit}><input className="input" required placeholder="Reset token" value={token} onChange={e=>setToken(e.target.value)}/><input className="input" required minLength={12} type="password" placeholder="New password" value={pw} onChange={e=>setPw(e.target.value)}/><input className="input" required minLength={12} type="password" placeholder="Confirm new password" value={confirm} onChange={e=>setConfirm(e.target.value)}/>{error&&<p className="error">{error}</p>}{done&&<p>Password updated. Redirecting to login…</p>}<button className="btn primary" disabled={done}>Set new password</button></form></div></main>}
