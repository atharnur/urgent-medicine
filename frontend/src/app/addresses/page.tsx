"use client";
import {useEffect,useState} from "react"; import {api} from "../../lib/api";
export default function Addresses(){const[data,setData]=useState<any[]>([]);const[label,setLabel]=useState("Home");const[line1,setLine1]=useState("");const[city,setCity]=useState("");const[postalCode,setPostal]=useState("");async function load(){const x=await api<any>("/customers/me/addresses");setData(x.data)}useEffect(()=>{load().catch(()=>{})},[]);async function add(){await api("/customers/me/addresses",{method:"POST",body:JSON.stringify({label,line1,city,postalCode})});setLine1("");setCity("");setPostal("");load()}return <main className="container"><div className="card"><h1>Addresses</h1><input className="input" placeholder="Label" value={label} onChange={e=>setLabel(e.target.value)}/><input className="input" placeholder="Address" value={line1} onChange={e=>setLine1(e.target.value)}/><input className="input" placeholder="City" value={city} onChange={e=>setCity(e.target.value)}/><input className="input" placeholder="Postal code" value={postalCode} onChange={e=>setPostal(e.target.value)}/><button className="btn primary" onClick={add}>Add address</button>{data.map(a=><p key={a.id}>{a.label}: {a.line1}, {a.city} {a.postalCode}</p>)}</div></main>}
