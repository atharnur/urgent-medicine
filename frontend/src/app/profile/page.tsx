"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "../../lib/api";
export default function Profile(){const router=useRouter();const[currentPassword,setCurrentPassword]=useState(""),[password,setPassword]=useState(""),[confirmPassword,setConfirmPassword]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");setMessage("");try{const r=await api<any>("/auth/change-password",{method:"POST",body:JSON.stringify({currentPassword,password,confirmPassword})});setMessage(r.message);setTimeout(()=>router.push("/login"),900)}catch(e:any){setError(e.message)}}return <main className="container"><div className="card" style={{maxWidth:520}}><h1>Profile & security</h1><h2>Change password</h2><form onSubmit={submit}><label>Current password<input className="input" required type="password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)}/></label><label>New password<input className="input" required minLength={12} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm new password<input className="input" required minLength={12} type="password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)}/></label>{error&&<p className="error">{error}</p>}{message&&<p>{message}</p>}<button className="btn primary">Change password</button></form></div></main>}
