"use client";
import { FormEvent, useState } from "react";
import { api } from "../../lib/api";
export default function ForgotPassword(){const[email,setEmail]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");try{const r=await api<any>("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})});setMessage(r.message)}catch(e:any){setError(e.message)}}return <main className="container"><div className="card" style={{maxWidth:480,margin:"40px auto"}}><h1>Forgot password</h1><p className="muted">Enter your email. For security, the response is the same whether an account exists or not.</p><form onSubmit={submit}><input className="input" required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/>{error&&<p className="error">{error}</p>}{message&&<p>{message}</p>}<button className="btn primary">Request reset</button></form><p><a href="/login">Back to login</a></p></div></main>}
